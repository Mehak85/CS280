#include <iostream>
#include <string>
using namespace std;

string DecToBin(int num)
{
    if(num==1)//base case 1;
    
      return "1";
    
    else if(num==0)//base case 2;
    
      return "0";
    
    else//general case
       return DecToBin(num/2) + to_string(num%2);
    
}
